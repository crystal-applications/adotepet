import { ThemedText } from '@/components/ThemedText';
import { SafeAreaView } from 'react-native-safe-area-context';

export default function FavoriteScreen() {
  return (
    <SafeAreaView>
      <ThemedText type="h1">Favorite Screen</ThemedText>
    </SafeAreaView>
  );
}
